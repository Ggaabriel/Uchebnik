при любом запросе на сервер к нему автоматом прикрепляется cookie но она пустая
если мы отправляем логин и тд то в куки лежит наш индетификатор
потом по индентификатору сервер дает доступ 

мы делаем loginReducer
в них копируем элементы с сервкака
https://social-network.samuraijs.com/api/1.0/auth/me

делаем функцию которая должна в action вернуть объект с 3 элементами которые мы скопировали

добавляем редюсер в стор

делаем в хедере навлинк login 

делаем хедер контейнер 
делаем в контейнере запрос на:
https://social-network.samuraijs.com/api/1.0/auth/me\
чтобы быть авторизованными мы пищем после ссылки через запятую withCredentials: true типол мы авторизованный доступ

далее делаем проверку на то что мы залогинены

if (answer.data.resultCode === 0){
	let {id, login, email} = answer.data.data; - деструктуризация
	this.props.setLogin(id, email, login)
}
делаем в стэйте значение авторизации: auth:false

при логине высвечиваем ник