мы хотим сделать чтобы наши иконки профелей были кликабельными и по нажатию мы переходили в профиль и видели информацию о человеке
для этого картинки людей мы делаем ссылками то есть navlink 
в линке мы делаем ссылку с айдишкой пользователя: to={`/profile/${u.id}

нам нужно как то менять людей на компоненте profile поэтому мы делаем в редюсере ключ profile:null который пустой
так же делаем установщик профиля который будет принимать профиль и записивать в пустой

после этого мы сделали profileContainer там делаем запрос на сервер а имено:
https://social-network.samuraijs.com/api/1.0/profile/2 --- мы получаем профиль Димыча 
и при получении устанавливаем профиль передавая data из api

тк профиль у нас изначально пустой мы делаем проверку:
if (!props.profile) {
        return <Loader />
    }
else{
	...
}
чтобы у нас пока не загрузится профиль был прелоудер