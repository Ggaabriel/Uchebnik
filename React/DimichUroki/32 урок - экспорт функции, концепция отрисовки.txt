в реакте прицип добавления нового элемента в массив работает так же как и в опп
мы в исходном файле создаем функцию а потом в другом ею пользуемся

функцию мы создаем в state.js 
чтобы ее экспортировать мы теперь пишем ее без default, так она экспортируется очень точно
пример:
	import {addPost} from './redux/state';
изменить название то есть {addPost} уже нельзя как с default

-далее мы через props прокидываем нашу функцию к кнопке добавляем действие, но пока что наш UI ничего не отобразит,
но код сработает, тк у нас все отрисовывается в index.js только один раз