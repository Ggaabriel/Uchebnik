для чего нужен import react если он не используется?
тк мы пишем компоненты на jsx, а jsx не попадает в браузер, попадает обычный js, jsx превращается в js с помощью babel который
находится в react поэтому он нужен