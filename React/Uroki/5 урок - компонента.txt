Компонента - это функция возвращающая(return) разметку (jsx)
const Header = () => {return(<h1>hui</h1>); }
Компонента - это тег 

в public index.html есть div с id="root" - тут отрисовывается наш js код 

чтобы создавать компонента мы их пишем отдельно но наслаиваем внутри App 
чтобы это сделать пишем:
<Header(любое название) />