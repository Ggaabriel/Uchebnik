createAsyncThunk - функция которая принимает в параметрах название слайсера/нами названное действие например:'users/fetchUsers'
потом ассинхроную функцию то есть thunk и возвращает какие то данные
потом еще какие то параметры

далее переменная в которой лежит эта штука прогоняется в extraReducers 
