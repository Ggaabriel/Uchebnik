чтобы установить тс к нам на готовый проект или скачать с нуля пишем в поисковике
cra typescript - там будет офф сайт там берем нужную нам комманду и вставляем в консоль
далее чтобы пользоваться тс нужно писать разширение как .js/.jsx только .ts/tsx
.tsx - это расширение с разметкой реакта только на тс 

функциональные компоненты должны возвращать React.FC как тип
const Header: React.FC = (props) => {return <>'kok'</>}

React.useState() - чтобы сделать типизацию в локальном стэйте мы пишем до скобочек
<> и в них задаем тип нашей переменной, если это объект то какие в нем элементы и их тип
например: 
const [pizza, setPizza] = React.useState<{
	imageUrl: string;
	title: string;
	price: number;		
}>()